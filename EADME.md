cara menjalankan :

1. Setup Environment di terminal / Command Prompt (CMD)
2. MASUK KE FOLDER PROJECT : cd submissionFAD
3. INSTALL DEPENDENCIES : pip install -r requirements.txt 
4. JALANKAN APLIKASI: streamlit run dashboard.py        

tapi Setup Environment di Terminal IDE seperti VSCODE (iDE yang saya gunakan)
 pip install -r requirements.txt
 streamlit run dashboard.py

5. bUKA DI BROWSER (biasanya akan otomatis terbuka di http://localhost:....)
 